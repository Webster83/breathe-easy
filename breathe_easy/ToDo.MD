# ToDo for v2 launch

- args for reset user, reset all, help
  - add info logging for 'called with args' so that no blank log is created on execution
- mask getting
  - need to change to set mask for this upload, add store as default prompt so that optional save default not assumed
- rewrite README.md (it's v1 and now a lot of confusion)
